# Chatbot App

![Chatbot App](/static/chatbot.png)

## Description

A ChatGPT-like interactive chatbot using SvelteKit, Daisy UI and the OpenAI API.

## Setup

- run `npm install`
- add `.env` file with `VITE_OPENAI_API_KEY` variable in root directory
- run `npm run dev`
